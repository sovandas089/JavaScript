const accountId = 890212
let accountEmail = "sovan@gamil.com"
var accountPassword = "12345"
accountCity = "Bilaspur"
let accountState;

// const = constant will never change if it declared once
/*
Prefer not to use var variable
Because of issue in block scope and functional scope
*/

console.table([accountId, accountEmail, accountPassword, accountCity, accountState])

// console.table is used for getting output in a table 